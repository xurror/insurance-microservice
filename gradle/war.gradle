apply plugin: "war"

bootWar {
    mainClassName = "com.apache.fineract.cn.insurance.InsuranceModuleApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
